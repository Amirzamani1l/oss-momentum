# راهنمای راه‌اندازی — Ecosystem Radar

پروژه‌ای که ربات هر روز واقعاً رویش کار می‌کند: از GitHub API و PyPI
دادهٔ ۴۲ پروژهٔ متن‌باز مهم را می‌گیرد، سری‌زمانی می‌سازد، شتاب رشد را
حساب می‌کند و README را به‌روز می‌کند.

جمعه‌ها (به وقت تهران) کار نمی‌کند.

---

## گام ۱ — ریپو بساز

یک ریپوی جدید بساز. **Public بگذارش** — چون:

- کارفرما می‌تواند ببیندش (کل هدف همین است)
- Actions روی ریپوی public نامحدود است، روی private ماهی ۲۰۰۰ دقیقه

اسم پیشنهادی: `ecosystem-radar`

---

## گام ۲ — فایل‌ها را بریز

کل محتویات این پوشه را در ریشهٔ ریپو کپی کن و push کن:

```bash
git add .
git commit -m "initial commit"
git push
```

اگر با سایت گیت‌هاب کار می‌کنی، یادت باشد پوشهٔ `.github` را نمی‌شود
drag & drop کرد. باید `Add file → Create new file` بزنی و در اسم فایل
تایپ کنی `.github/workflows/ci.yml` تا پوشه خودکار ساخته شود.

---

## گام ۳ — ایمیلت را به‌عنوان متغیر ثبت کن

این مهم‌ترین گام است. بدون آن کامیت‌ها به اسم ربات ثبت می‌شوند و
**سبز نمی‌شوی**.

۱. برو `https://github.com/settings/emails` و ایمیل noreply خودت را
   کپی کن (شکلش: `12345678+Amirzamani1l@users.noreply.github.com`)

۲. برو در **ریپو** → `Settings` → `Secrets and variables` → `Actions`

۳. تب **Variables** را انتخاب کن (نه Secrets)

۴. `New repository variable`:

   | فیلد | مقدار |
   |---|---|
   | Name | `GIT_EMAIL` |
   | Value | ایمیل noreply تو |

اسم باید دقیقاً `GIT_EMAIL` باشد.

> چرا Variable و نه Secret؟ چون ایمیل noreply محرمانه نیست و اگر Secret
> باشد گیت‌هاب در لاگ‌ها سانسورش می‌کند و عیب‌یابی سخت می‌شود.

---

## گام ۴ — اجازهٔ نوشتن بده

`Settings` → `Actions` → `General` → پایین صفحه، بخش
**Workflow permissions** → گزینهٔ **Read and write permissions** را بزن
→ `Save`

بدون این، workflow نمی‌تواند push کند.

---

## گام ۵ — اولین اجرا

`Actions` → `Collect` → `Run workflow` → دوباره `Run workflow`

حدود یک دقیقه طول می‌کشد. بعدش:

- `data/observations.csv` باید ساخته شده باشد (۴۲ ردیف)
- `charts/categories.svg` باید ساخته شده باشد
- README باید پر شده باشد

اگر امروز جمعه است، workflow اجرا می‌شود ولی هیچ کاری نمی‌کند. یا صبر کن
یا موقتاً آن شرط را از فایل بردار.

---

## گام ۶ — چک نهایی

برو تب Commits. آخرین کامیت باید پیامی مثل این داشته باشد:

```
data: 2026-08-23 snapshot (42 projects, +1,204 stars, polars leading)
```

- ✅ آواتار **خودت** کنارش → درست است
- ❌ `github-actions[bot]` → متغیر `GIT_EMAIL` غلط یا خالی است

---

## گام ۷ — بج‌ها را درست کن

در `README.md` دو خط بالا، `OWNER/REPO` را با یوزرنیم و اسم ریپوی
خودت عوض کن تا بج‌های CI کار کنند.

---

## چه چیزی خودکار اتفاق می‌افتد

| کِی | چه اتفاقی |
|---|---|
| ۰۱:۱۷، ۰۹:۴۳، ۱۷:۲۹ UTC | دادهٔ تازه، کامیت جدید |
| جمعه‌ها | هیچی |
| هر push | CI روی سه نسخهٔ پایتون تست می‌گیرد |

روزی ۱ تا ۳ کامیت. یعنی حدود ۲۰ کامیت واقعی در هفته.

---

## اجرای محلی (اختیاری)

```bash
pip install -r requirements-dev.txt

pytest                                    # ۱۲۲ تست
ruff check . && ruff format --check .     # لینت

# سه پروژه را بگیر و گزارش را چاپ کن، بدون نوشتن چیزی
GITHUB_TOKEN=توکنت python scripts/collect.py --dry-run --limit 3
```

---

## عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| `Permission denied` هنگام push | گام ۴ را انجام نداده‌ای |
| کامیت به اسم ربات ثبت می‌شود | متغیر `GIT_EMAIL` غلط است — گام ۳ |
| `403 rate limit` | نباید پیش بیاید؛ اگر شد یعنی `GITHUB_TOKEN` پاس نشده |
| CI قرمز است ولی Collect سبز | تست‌ها شکسته‌اند، نه خط لوله. `pytest -v` را محلی بزن |
| بعد از ۶۰ روز خاموش شد | گیت‌هاب schedule ریپوهای کم‌فعالیت را غیرفعال می‌کند. یک بار دستی Run workflow بزن |
| نمودار ۳۰ روزه خالی است | طبیعی است، تا ۳۰ روز اول داده کافی نیست |

---

## اگر خواستی شخصی‌ترش کنی

**افزودن پروژه:** یک خط در `src/radar/config.py`:

```python
Project("owner/name", "data-engineering", "pypi-package-name"),
```

**تغییر آستانه‌ها:** بالای `src/radar/analyze.py`:

```python
STALE_PUSH_DAYS = 45
ISSUE_PRESSURE_WARN = 25.0
```

**تغییر ساعت‌ها:** بخش `cron` در `.github/workflows/collect.yml`

---

## یک نکتهٔ صادقانه

این ریپو واقعی است — کد واقعی، تست واقعی، دادهٔ واقعی. ولی اگر کارفرمایی
ازت دربارهٔ آن سؤال کند، باید بتوانی توضیح بدهی چرا `growth_30d_pct` را
به‌جای تعداد ستاره انتخاب کردی، یا چرا دلتاها به تاریخ لنگر شده‌اند نه
به شمارهٔ ردیف.

جواب هر دو در `docs/METHODOLOGY.md` هست. یک بار بخوانش. ده دقیقه وقت
می‌برد و فرق بین «پروژهٔ من» و «پروژه‌ای که یکی برایم ساخت» همین است.
